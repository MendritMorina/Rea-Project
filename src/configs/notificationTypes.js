// Helper config objects.
const NEW_COUPON = { name: 'NEW_COUPON', showName: 'New Coupon' };
const NEW_STORY = { name: 'NEW_STORY', showName: 'New Story' };
const GENERAL = { name: 'GENERAL', showName: 'General' };

// Config array that is used to store types of notifications used in our API.
const config = [NEW_COUPON, NEW_STORY, GENERAL];

// Exports of this file.
module.exports = config;
